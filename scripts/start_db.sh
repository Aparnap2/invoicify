#!/bin/bash
# Start D1 Database (SQLite via wrangler dev)
# D1 runs locally through wrangler, no Docker needed for local dev

echo "📝 Note: D1 runs locally via wrangler dev"
echo "Use: wrangler d1 migrations apply invoicify-db --local"
